

class Scanner:
  def __init__(self, source):
    tokens = []
    self.source = source


def isAtEnd():
  return current >= len(source)

def ScanTokens():
  while (!isAtEnd()):
    start = current
    scanToken()

  

